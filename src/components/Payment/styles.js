import styled from 'styled-components';

const styles = {};
styles.PaymentWrapper = styled.div``;

styles.ContentWrapper = styled.div`
  width: 940px;
  margin: 24px auto 40px;
`;

styles.LoaderWrapper = styled.div`
  width: 940px;
  text-align: center;
  position: relative;
  padding: 150px 0;
  margin: 0 auto;
`;

export default styles;
