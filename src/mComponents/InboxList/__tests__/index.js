// import React from 'react';
// import { createMount } from '../../../test-utils';
// import CardItems from './utils/factory';
// import InboxList from '../../InboxList';
// import { Card } from '../Card';

// jest.mock('../../Common/Link');
// describe('invites', () => {
//   const props = {
//     results: CardItems.connect_pending.results,
//     profiles: CardItems.connect_pending.profiles,
//     requestType: CardItems.connect_pending.requestType,
//     settings: CardItems.settings,
//     onAction: jest.fn(),
//   };
//   // const inboxList = createMount()(<InboxList {...props} />);
//   it(`Should Render ${props.results.items.length} items`, () => {
//     // expect(inboxList.find(Card).length).toBe(props.results.items.length);
//   });
// });
