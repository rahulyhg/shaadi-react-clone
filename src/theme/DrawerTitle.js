import styled from 'styled-components';

const DrawerTitle = styled.div`
  color: #51505d;
  font-family: Roboto;
  font-size: 16px;
  display: inline-flex;
  box-shadow: 0 2px 2px rgba(0, 0, 0, 0.24), 0 0 2px rgba(0, 0, 0, 0.1);
`;

export default DrawerTitle;
